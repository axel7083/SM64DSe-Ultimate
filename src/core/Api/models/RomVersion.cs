namespace SM64DSe.core.Api.models
{
    public class RomVersion
    {
        public string version;

        public RomVersion(string version)
        {
            this.version = version;
        }
    }
}